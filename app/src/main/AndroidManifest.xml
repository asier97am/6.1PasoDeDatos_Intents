<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a61conversion_a_binario">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.61Conversion_a_binario">
        <activity
            android:name=".activity_unidades"
            android:exported="true" />
        <activity
            android:name=".activity_decenas"
            android:exported="true" />
        <activity
            android:name=".activity_centenas"
            android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".activityResultado"
            android:exported="true">

        </activity>
    </application>

</manifest>